# message1

Hello from vc-x1. This specimen is what the vc-x1-messages README's handled example points at.
It is the sibling of iiac-perf's `messages/test-msg.md`, sitting under `notes/messages/` instead
to show that a body lives wherever its sender chooses.
